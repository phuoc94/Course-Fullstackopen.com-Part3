{
  "name": "puhluettelobackend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "cd ../Fullstack/osa2/puhelinluettelo && npm run build --prod && ",
    "deploy": "git push heroku main",
    "deploy:full": "git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Phuoc Nguyen",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
